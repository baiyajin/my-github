# 个人作品与技术展示网站

一个现代化的个人作品和技术展示网站，使用最新的前端技术栈构建。

## 技术栈

- **Vite 7** - 下一代前端构建工具
- **Vue 3** - 渐进式 JavaScript 框架（启用 Vapor Mode）
- **UnoCSS** - 即时原子化 CSS 引擎
- **Three.js** - 3D 图形库
- **WebAssembly** - 高性能计算支持
- **JavaScript** - 主要开发语言

## 功能特性

- 🎨 **3D 背景动画** - 使用 Three.js 创建的动态粒子系统和几何体
- 📱 **响应式设计** - 完美适配各种设备
- ⚡ **高性能** - Vapor Mode 和 WebAssembly 优化
- 🎯 **现代化 UI** - 使用 UnoCSS 构建的优雅界面
- 🚀 **快速加载** - Vite 7 提供极速开发体验

## 项目结构

```
.
├── src/
│   ├── components/          # Vue 组件
│   │   ├── NavBar.vue       # 导航栏
│   │   ├── HeroSection.vue  # 首页 Hero 区域
│   │   ├── ThreeBackground.vue  # Three.js 背景
│   │   ├── ProjectsSection.vue  # 作品展示
│   │   ├── TechStackSection.vue # 技术栈展示
│   │   ├── WasmDemoSection.vue  # WebAssembly 演示
│   │   └── ...
│   ├── App.vue              # 主应用组件
│   ├── main.js              # 入口文件
│   └── style.css            # 全局样式
├── index.html               # HTML 模板
├── vite.config.js           # Vite 配置
├── uno.config.js            # UnoCSS 配置
└── package.json             # 项目依赖

```

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

构建产物在 `dist` 目录

### 预览构建结果

```bash
npm run preview
```

## 自定义内容

项目使用假数据填充，你可以替换以下内容：

1. **个人信息** - 编辑 `src/components/HeroSection.vue`
2. **作品项目** - 编辑 `src/components/ProjectsSection.vue`
3. **技术栈** - 编辑 `src/components/TechStackSection.vue`
4. **联系信息** - 编辑 `src/components/ContactSection.vue`

## 部署

构建后的静态文件可以部署到：

- **GitHub Pages**
- **Netlify**
- **Vercel**
- **Cloudflare Pages**

无需服务器，直接部署 `dist` 目录即可。

## 注意事项

- Vue 3 Vapor Mode 目前处于 alpha 阶段
- 确保所有组件使用 `<script setup>` 语法
- WebAssembly 模块需要根据实际需求进行开发

## 许可证

MIT

